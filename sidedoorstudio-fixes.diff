diff --git a/css/style.css b/css/style.css
index baef7db..146f0c3 100644
--- a/css/style.css
+++ b/css/style.css
@@ -29,6 +29,7 @@
 }
 
 *, *::before, *::after{ box-sizing: border-box; }
+figure{ margin: 0; }
 html{ scroll-behavior: smooth; }
 @media (prefers-reduced-motion: reduce){
   html{ scroll-behavior: auto; }
@@ -404,8 +405,9 @@ section.tight{ padding: 64px 0; }
   color: rgba(212,200,176,0.62);
   margin: 0;
 }
-@media (max-width: 900px){ .services-grid{ grid-template-columns: repeat(2,1fr); } }
-@media (max-width: 620px){ .services-grid{ grid-template-columns: 1fr; } }
+.process-grid{ grid-template-columns: repeat(4,1fr); }
+@media (max-width: 900px){ .services-grid, .process-grid{ grid-template-columns: repeat(2,1fr); } }
+@media (max-width: 620px){ .services-grid, .process-grid{ grid-template-columns: 1fr; } }
 
 /* ---------- Work grid ---------- */
 .work-grid{
diff --git a/services.html b/services.html
index ef39d91..95bafd2 100644
--- a/services.html
+++ b/services.html
@@ -150,7 +150,7 @@
       <p>The same person who scopes the work is the person building it, from the first call to launch.</p>
     </div>
 
-    <div class="services-grid reveal" style="grid-template-columns:repeat(4,1fr);">
+    <div class="services-grid process-grid reveal">
       <div class="service-card"><div class="num">01</div><h3>Discovery</h3><p>A direct conversation about what the business actually needs, not a sales deck.</p></div>
       <div class="service-card"><div class="num">02</div><h3>Design</h3><p>A visual direction built for this brief only, reviewed with you before anything is coded.</p></div>
       <div class="service-card"><div class="num">03</div><h3>Build</h3><p>Development in the open, with working previews as it comes together.</p></div>
